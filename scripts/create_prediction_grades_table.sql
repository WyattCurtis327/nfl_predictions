-- Empty placeholder so game_pick_metrics can bind before the first grade run.
CREATE TABLE IF NOT EXISTS nfl.predictions.prediction_grades (
  grade_id STRING,
  grading_run_id STRING,
  prediction_id STRING,
  prediction_run_id STRING,
  mlflow_run_id STRING,
  season BIGINT,
  week BIGINT,
  game_type STRING,
  gameday STRING,
  game_id STRING,
  away_abbr STRING,
  home_abbr STRING,
  spread_pick STRING,
  total_pick STRING,
  spread_confidence DOUBLE,
  total_confidence DOUBLE,
  away_spread DOUBLE,
  home_spread DOUBLE,
  total_line DOUBLE,
  proj_away_score DOUBLE,
  proj_home_score DOUBLE,
  proj_total DOUBLE,
  actual_away_score DOUBLE,
  actual_home_score DOUBLE,
  actual_total DOUBLE,
  actual_winner STRING,
  actual_spread_result STRING,
  actual_total_result STRING,
  spread_correct BOOLEAN,
  total_correct BOOLEAN,
  spread_push BOOLEAN,
  total_push BOOLEAN,
  away_score_error DOUBLE,
  home_score_error DOUBLE,
  total_error DOUBLE,
  graded_at STRING
) USING DELTA